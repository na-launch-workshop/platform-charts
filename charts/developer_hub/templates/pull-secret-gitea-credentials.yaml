## TODO: Will need to build Ansible playbook to call Gitea API in a similar way as GitLab below,
##       so that we can use this group_access_token
##
##       platform-ansible-collections/bootstrap/workshop/roles/gitlab/tasks/main.yaml

#{{- if .Values.gitea.enabled }}
#apiVersion: external-secrets.io/v1beta1
#kind: ExternalSecret
#metadata:
#  name: gitea
#spec:
#  refreshInterval: "2m"
#  secretStoreRef:
#    name: eso-admin-secret-store
#    kind: ClusterSecretStore
#  target:
#    name: gitea-token
#  data:
#  - secretKey: GITEA_TOKEN
#    remoteRef:
#      key: gitea
#      property: group_access_token
#{{- end }}
